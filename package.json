{
  "name": "phrain",
  "version": "1.0.2",
  "description": "wordpress_plugin",
  "main": "index.js",
  "scripts": {
    "compile": "node -e \"require('./index.js').handler()\" && zip -r ./phrain.zip ./src",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "moment": "^2.29.4"
  }
}
